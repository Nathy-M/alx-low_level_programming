#include "main.h"
/**
 *  _abs - calculates absolute value of number
 *  @n: number to cm=omputed
 *  Return: Returns 0 for success
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
