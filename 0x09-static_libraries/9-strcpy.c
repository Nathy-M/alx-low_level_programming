#include "main.h"
#include <string.h>
/**
 * _strcpy - function that copies the string
 * @dest: destination point
 * @src: source
 * Return: returns char
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
