#include "main.h"
#include <string.h>
/**
 * print_rev - prints string in reverse direction
 * @s: passed string
 */
void print_rev(char *s)
{
	char *temp_str;
	int count;

	for (count = s.length() - 1; count > 0; count--)
	{
		*temp_str = *s(count);
	}
}
