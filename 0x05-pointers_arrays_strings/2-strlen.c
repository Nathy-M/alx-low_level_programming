#include "main.h"
/**
 * _strlen - returns length of string
 * @s: a charcter argument
 * Return: returns string length
 */
int _strlen(char *s)
{
	int str_len;

	str_len = strlen(*s);
	return str_len;
}
